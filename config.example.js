/**
 * Supabase / cloud save configuration.
 * Copy this file to config.js and set your project URL and anon key.
 * Get them from: Supabase Dashboard → Settings → API.
 * If config.js is missing or uses placeholders, the app runs in local-only mode.
 */
window.__supabaseConfig = {
  url: "https://YOUR_PROJECT_REF.supabase.co",
  anonKey: "YOUR_ANON_KEY"
};
